# 商品成本“快速录入”实施简报

**范围：交互与数据设计，暂不执行代码改造。**  
**目标：让第一次给商品建立成本时，商户只填写一个必填金额与一个可选金额；复杂明细保留但不阻塞录入。**

## 1. 推荐最终结构

参考 Square 的“商品类型决定动态属性”、采购单中按真实单位成本建立商品，以及金蝶/聚商荟将商品档案、采购事实和经营分析分层的做法，建议“算得清”采用 **快速成本 → 进阶明细 → 经营分摊** 三层结构。[1][2][3][4]

| 层级 | 用户目标 | 页面只做什么 | 用户输入数量 |
|---|---|---|---:|
| 快速成本 | 先知道这件商品能不能赚钱 | 建立当前单件直接成本 | 1 个必填金额 + 1 个可选金额 |
| 进阶明细 | 精确拆材料、损耗和批量制作 | 使用材料、配方、出成量、损耗与成本版本 | 不设上限，但由用户主动进入 |
| 经营分摊 | 看完整经营利润 | 录入/归集房租、利息、月度配送等期间成本 | 不在商品页逐件填写 |

## 2. 商品页应如何恢复入口

当前“编辑进货明细/编辑配方”是正确功能，但它表达的是编辑动作，不是用户最想做的“把成本录进去”。并且“新增自定义项目”需要在弹层底部切换类型后才可见，造成入口消失的感受。

| 商品状态 | 主按钮（高强调） | 次按钮（低强调） | 解释文案 |
|---|---|---|---|
| 尚未建立成本 | `录入成本` | `使用材料明细` | `先填 1–2 项，约 30 秒` |
| 已有快速成本 | `更新成本` | `查看/编辑明细` | `当前成本 ¥x / 件` |
| 已有进阶明细 | `更新成本` | `编辑材料明细` | `已按 x 项材料、损耗 x% 核算` |

商品详情页不再放“新增项目”这个术语。因为对商户而言，他想增加的是**成本**，不是一个抽象的“项目”。“新增项目”仅保留在进阶明细中，并改成更清楚的 `添加材料` 与 `添加其他单件成本`。

## 3. 快速录入页草图（文字版）

```text
← 录入商品成本                         约 30 秒
招牌奶茶 · 先填两项，先算清一件赚不赚

【必填】这件商品的主要成本是多少？
  食材成本                         ¥ [        ] / 份
  有真实采购记录时： [采用最近采购价 ¥3.20]

【可选】每卖一件还会多花什么？
  [包装费 v]                       ¥ [        ] / 份
  标签可切换：餐饮=包装费/单件人工；零售=配送费；
             摆摊=摊位交通分摊；手作=直接人工/包材费

当前直接成本：¥3.20 / 份
[保存并生成成本版本]

已有多种材料、损耗或批量制作？ [展开进阶明细]
```

默认只显示上述两张卡。第二张卡不填写即为 **¥0**，但 UI 必须显示“暂未计入”，不能把空值描述为“已估算”。单位由行业模板和商品类型预设为“件/份”，用户不再输入单位文本。

## 4. 四行业预设

| 行业 | 主成本卡 | 可选附加卡默认标签 | 允许切换的预设标签 | 不在商品页录入 |
|---|---|---|---|---|
| 餐饮饮品 | 食材成本 / 份 | 包装费 / 份 | 包装费、单件人工 | 房租水电、平台抽佣、店主工时 |
| 社区零售 | 进货价 / 件 | 单件配送费 / 件 | 单件配送费、促销让利 | 平台费率、货架占用、月度物流 |
| 商贸摆摊 | 拿货价 / 件 | 摊位交通分摊 / 件 | 摊位交通分摊、尾货损耗 | 月度摊位费、全程交通费 |
| 手作生产 | 材料成本 / 件 | 直接人工 / 件 | 直接人工、包材费 | 工具折旧、试做报废 |

行业标签固定为预设，用户不能在快速页自由新建名称。这避免“运费”“配送费”“平台费”等同义成本在不同商品里重复计入。确有行业特定项目时，用户仍在“我的 → 成本项目”统一维护；新增的类别只在进阶明细或经营账入口使用。

## 5. 数据与会计口径约束

快速录入只是**更少的输入面**，不是另一套账。一次快速保存必须继续走当前商品成本版本快照流程，生成包含直接成本、包装、人工、损耗、出成量和成本明细的版本；销售发生时仍使用当时快照。

| 情况 | 数据处理建议 | 原因 |
|---|---|---|
| 用户手填主成本 | 写入系统标记的“主成本”明细，来源标注为 `manual` | 手填成本需要稳定留在当前产品版本中，后续不会被材料改价静默改写 |
| 用户点击“采用最近采购价” | 让用户明确选择某条真实材料/采购记录；保存来源标注为 `material` 或 `purchase` | 保留来源可追溯性，并允许现有的材料改价逻辑只重算当前商品成本 |
| 用户填写附加成本 | 根据预设标签写入包装、直接人工或系统标记的单件附加明细 | 确保一笔金额只计入一个成本位置，杜绝同时写入 BOM 与包装/人工的双计 |
| 用户不填附加成本 | 保存为 0 且显示“暂未计入” | 不用行业均值或演示金额填补，保持账本诚实 |
| 用户展开进阶明细 | 打开当前 BOM 编辑器；保存时新建成本版本 | 复用已验证的材料、损耗、出成量与历史版本机制 |
| 房租、利息、月度配送等 | 留在经营账/资金成本并按期间分摊 | 不让用户把期间成本逐商品重复手填 |

建议在 `BomItem` 或对应产品成本结构中新增内部字段，例如 `entryMode: "quick" | "advanced"` 和 `presetId`，用来区分“用户自由自定义”与“系统行业预设”。展示层可将系统预设汇总为两张成本卡，进阶层仍可展开实际组成。旧数据保持兼容：已有 BOM 不自动转成快速成本；用户主动改为快速模式时才新增版本。

## 6. P0 开发切片与验收

| 顺序 | 改动 | 关键验收 |
|---:|---|---|
| 1 | 商品详情将主操作替换为“录入/更新成本”，并保留“编辑材料明细”次操作 | 任何商品从详情进入成本页只需 1 次点击；页面上不再要求用户寻找“新增项目” |
| 2 | 新建快速成本弹层与四行业两张成本卡 | 首次页面无名称、单位、数量文本框；最多 2 个金额输入；主成本为空不能保存 |
| 3 | 复用当前成本快照保存链路 | 快速保存会生成新成本版本；历史销售的成本和利润不变化 |
| 4 | 增加“采用最近真实采购价” | 无真实采购时不显示；有真实采购时也必须用户点击确认；不采用演示价 |
| 5 | 将现有 BOM 编辑器明确命名为“进阶明细” | 材料、多项目、损耗、出成量和历史恢复全部继续可用 |

## 7. 不应照搬的主流功能

大型进销存会加入 SKU、条码、供应商、仓库、门店、采购单、批次、价格策略、促销、会员和多渠道同步。它们适合库存交易管理，但会让“算得清”的首轮成本核算变成后台建档工程。当前版本只应借鉴其**分层原则、真实采购来源与动态字段**，不复制其复杂工作流。

同样不应照搬电商平台的自动定价、竞品监控或按市场调价。对算得清的当前目标而言，先确保每件商品的成本来源准确、期间成本不重复、历史销售不漂移，比自动调价更重要。

## 参考资料

[1] [Square — Create and edit items](https://squareup.com/help/us/en/article/8335-create-and-edit-items)  
[2] [Square — Create and manage purchase orders](https://squareup.com/help/us/en/article/8258-create-purchase-orders-with-square-for-retail)  
[3] [金蝶精斗云·云进销存](https://www.kingdee.com/products/yjxc.html)  
[4] [聚商荟帮助中心](https://help-jsh.paidui.com/)  
[5] [Shopify — Pricing your products](https://help.shopify.com/en/manual/products/details/product-pricing/determine-pricing)
