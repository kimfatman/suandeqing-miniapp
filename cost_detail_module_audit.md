# 成本明细模块审查结论

## 本轮阻塞问题

自定义成本项目的底层保存链路本身可以写入 BOM，但原界面对用户不够确定：新行没有自动聚焦，名称、单位、单价和数量错误只有一条笼统提示，且保存前的总成本没有包含包装和直接人工，容易让用户误以为填完仍然无法保存或金额计算错误。

本轮已将自定义成本行改为保存前自动清理名称与单位空格；新建后自动聚焦项目名称；保存失败后为每一行显示具体缺失项；同时显示本行成本。真实 390px 浏览器回归已填写“平台服务费 / 单 / 1.5 / 2”并成功保存到商品 BOM，未返回错误。

## 计算与历史检查

直接成本预览现在同时包含材料/自定义成本、损耗、出成、包装和直接人工，因此保存前后口径一致。历史版本恢复会同时带回材料单价、包装、直接人工、损耗和出成；一旦用户修改任何成本明细，历史快照预览会被解除，新的保存会生成新版本。材料改价仅重算当前成本，不会改写历史销售快照。

## 交互问题与处理结果

| 场景 | 原风险 | 本轮处理 |
|---|---|---|
| 新增自定义项目 | 焦点不明确，用户需手动定位字段 | 自动聚焦项目名称 |
| 任何字段漏填 | 只显示统一报错，难以找到原因 | 标红对应行并显示“名称/单位/单价/数量”具体缺口 |
| 保存前金额 | 不含包装和直接人工 | 改为“当前直接成本预览”，保存前后金额一致 |
| 无已录入材料 | 选择“已有材料”后无操作反馈 | 禁用新增并提示可先录入材料或切换自定义成本 |
| 移动端行布局 | 容易发生字段拥挤 | 名称独占、单位/单价分栏、数量独立，保存区保持可见 |

## 后续非阻塞增强

下一步可增加“包装费”“直接人工”“平台服务费”的快捷模板，以减少手填；支持复制一行和对自定义项目排序；在历史版本列表显示每次变更的项目差异。这些功能会改善效率，但不影响当前自定义成本的保存和核算正确性。
