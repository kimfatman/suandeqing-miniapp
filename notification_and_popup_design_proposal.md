# “算得清”消息提醒与弹窗交互设计方案

**状态：方案待确认，尚未开发。**  
**目标：** 让运营后台能够向已登录的商户发送有价值、可追踪的站内消息；同时用克制的弹窗帮助用户完成必要操作，而不打断日常记账。

## 1. 结论与推荐范围

首期建议采用 **“站内消息中心 + 未读角标 + 有条件的轻提示/确认弹窗”**。运营人员可通过受管理员权限保护的后台向指定商户或已登录商户群发消息；客户在“我的”页看到消息入口、未读数量和消息详情。消息可携带一个明确动作，例如“查看说明”“去备份”或“更新账本”。

**首期不建议把普通运营公告做成启动即弹的全屏弹窗，也不建议依赖自动外部推送。** 当前“算得清”是基于 Web 的应用，且账本采用本地优先、手动备份的模型；后端并不完整掌握所有商户的实时经营数据。因此，第一版应优先解决“后台能准确投递、客户能查看和处理、投递可追溯”，而不是承诺尚无数据来源支撑的自动经营预警。

微信小程序的外部订阅消息需要用户主动订阅，消息进入微信“服务通知”，并可跳转回小程序页面。[1] 一次性订阅在用户授权后只能发送一条对应服务消息；长期订阅目前只向特定公共服务行业开放。[2] 因此，**微信服务通知应作为第二期、用户主动开启后的补充通道**，不能替代首期站内消息。

| 能力层 | 首期是否开发 | 面向谁 | 典型内容 | 用户打断强度 |
|---|---:|---|---|---|
| 站内消息中心 | 是 | 已登录商户 | 功能更新、账本安全提示、运营公告 | 无，用户主动查看 |
| 未读角标与页内提示条 | 是 | 有未读且与当前页相关的商户 | “有 1 条账本安全提醒” | 低 |
| 操作结果 Toast | 是 | 当前操作用户 | “已标记为已读”“发送失败，请重试” | 极低 |
| 风险确认弹窗 | 是 | 即将执行高风险操作的商户 | 覆盖账本、删除消息、退出账号 | 高，仅需确认时出现 |
| 运营公告弹窗 | 有条件 | 收到“重要”公告的商户 | 服务规则重大变更、强制行动通知 | 中，每条只展示一次 |
| 微信订阅消息 | 否，列为二期 | 主动授权的微信小程序用户 | 到期提醒、服务闭环通知 | 应用外 |

## 2. 主流设计原则与对“算得清”的转译

Material Design 将 Dialog 定义为用于确保用户对重要信息、动作或高风险操作作出处理的组件，并强调一个对话框应服务于单一任务。[3] Apple 进一步建议，警告框应克制使用；仅用于提供信息时应改用相关上下文中的非打断性呈现，且不应在应用启动时直接打断用户。[4] 因而，“算得清”不应以“弹窗曝光”为目标，而应以“商户是否能在正确时间完成正确动作”为目标。

Shopify 的商家通知将事件、收件人、启停和频率分开配置，并允许在正式启用前测试提醒。[5] 这一做法适合借鉴为“运营后台可选目标范围、可预览、可撤回/停用”，但不应照搬复杂的邮件模板编辑器。首期后台只允许固定结构的标题、正文、跳转动作与有效期，避免运营配置出误导性或不一致的商户提醒。

> **产品原则：** 日常信息沉入消息中心；与当前任务有关的提醒在页面内出现；只有不可逆、强行动或合规性事项才使用确认弹窗。任何提醒都必须回答“发生了什么、为什么与我有关、下一步做什么”。

## 3. 消息分级与弹窗决策表

| 级别 | 内容示例 | 入口与呈现 | 是否自动弹出 | 主要动作 | 频率规则 |
|---|---|---|---:|---|---|
| A：行动安全 | “云端备份已过期，请先备份再换设备” | “我的”账户卡、消息详情页 | 否；仅在用户触发覆盖/恢复时确认 | 去备份、查看数据管理 | 同一事项 7 天内只提示一次 |
| B：重要公告 | “本月成本分类规则将在 8 月 31 日更新” | 消息中心 + 首次进入后顶部提示条 | 否；运营标记“重要”时一次性底部弹层 | 查看说明、知道了 | 每条仅自动呈现一次 |
| C：经营建议 | “你已有销售记录，但有 2 个商品尚未补齐成本” | 首页任务卡、消息中心 | 否 | 去补成本 | 由本地账本即时计算，不由后台群发 |
| D：普通资讯 | “新增手作生产行业模板说明” | 消息中心 | 否 | 查看详情 | 不重复打断 |
| E：操作反馈 | “已标记为已读”“暂时无法加载消息” | Toast | 否 | 无或重试 | 随操作出现、自动消失 |

这里要特别区分两类信息。**后台运营消息** 是管理员主动发送、与所有或部分商户相关的内容；**账本状态提醒** 则由本机账本在用户打开应用时计算，更适合留在首页任务卡或“我的”页，因为当前本地优先架构并不保证后端拥有最新账本。将两者混为“后台自动提醒”会造成过期或错误提示，违反本产品对核算准确性的优先级。

## 4. 客户端信息架构与弹窗草图

### 4.1 消息入口与消息中心

建议在顶栏右侧新增铃铛图标。存在未读时显示蓝色小圆点；未读数为 1–99 时显示数字，超过 99 显示 `99+`。点击后以底部 Sheet 打开“消息中心”，与现有成本、数据管理的移动端操作习惯保持一致。

```text
顶栏                                      [铃铛 · 2]

消息中心
────────────────────────────────
未读 2                         全部已读

●  账本安全
   建议为当前账本创建云端备份                 刚刚
   [去备份]

●  产品更新
   新增“手作生产”行业成本说明                昨天
   [查看说明]

   已读消息
   …
```

每条消息包含 **来源标签、短标题、最多两行摘要、时间、已读状态和一个主动作**。用户点开详情即标记已读；“全部已读”只改变阅读状态，不删除审计记录。详情页使用 Sheet，而不是新增深层路由，以保证用户在任何页面都能返回原任务。

### 4.2 自动提示与弹窗样式

普通消息不在冷启动时立即弹出。若存在一条此前未展示的 B 级重要公告，在用户完成首屏加载后以**顶部提示条**出现；用户可点“查看”进入详情，或关闭并稍后在消息中心查看。只有用户要进行覆盖账本、删除、退出等不可逆动作时，使用带“取消”的确认弹窗。

```text
┌──────────────────────────────────┐
│ 算得清提醒                        × │
│ 备份功能有更新，恢复前请确认来源。 │
│                         [查看说明] │
└──────────────────────────────────┘

确认覆盖账本
将用云端账本替换此设备上的账本；两份数据不会自动合并。
[取消]                    [用云端覆盖]
```

弹窗文案应使用具体动词，不使用模糊的“确定”。例如“用云端覆盖”“保留本机并备份”“查看说明”。确认框仅保留一个风险动作和一个安全退出动作；这与主流移动平台的可操作、明确按钮和取消路径原则一致。[3] [4]

## 5. 首期后台与数据设计

当前项目已经拥有 `users.role`、Manus OAuth、tRPC、MySQL/Drizzle 和 `protectedProcedure`，可在不引入第三方推送服务的前提下完成站内消息。现有 Manus 的所有者通知能力仅适合向**项目所有者**发送运营告警，不适合对商户客户群发；客户消息必须由应用自己的数据模型保存和读取。

建议新增以下最小模型。时间字段统一存储 UTC，在前端转换为用户本地时间显示。

| 表/对象 | 关键字段 | 用途与约束 |
|---|---|---|
| `message_campaigns` | `id`、`createdByUserId`、`title`、`body`、`level`、`actionLabel`、`actionPath`、`targetType`、`publishedAt`、`expiresAt`、`status` | 管理员编写的一次运营消息；`draft`、`published`、`recalled` 三态。 |
| `user_messages` | `id`、`campaignId`、`userId`、`readAt`、`displayedAt`、`dismissedAt`、`actionedAt` | 发送时把收件人快照写入本表，保证后续用户新增/角色变化不篡改历史投递范围。 |
| `notification_preferences` | `userId`、`productUpdatesEnabled`、`accountSafetyEnabled`、`marketingEnabled` | 首期仅保留三类总开关；**账本安全**默认开启但可改为只在站内展示。 |

后台路由采用明确的权限分层：`messages.list`、`messages.unreadCount`、`messages.markRead` 仅允许当前登录用户读取自己的记录；`admin.messages.create`、`preview`、`publish`、`recall` 仅允许 `role=admin`。发送前必须预览收件人数，发布后记录管理员、发布时间和目标范围；已发布消息允许撤回，但不物理删除历史投递记录。

```text
管理员后台
草稿 → 预览（目标人数/样式/跳转） → 发布 → 生成用户收件箱记录
                                               ↓
商户应用：未读角标 → 消息中心 → 查看详情 → 标记已读 / 执行动作
```

### 首期后台发送表单

管理员表单固定为：**消息类型、标题（≤24字）、摘要（≤56字）、正文、目标范围、跳转动作、有效期、重要标记**。不开放 HTML、外部追踪像素、自由 URL 和附件上传。跳转动作只允许白名单路径，如“首页”“我的-数据管理”“商品”，从而避免把消息系统变成任意外链入口。

## 6. 微信订阅消息的第二期边界

当项目真正接入微信小程序运行环境后，可增加“每月经营提醒”开关。用户必须在某个与业务相关的动作之后主动选择订阅；例如完成月度经营查看后，点击“开启下月提醒”。微信要求先在公众平台配置模板并取得模板 ID，再由前端获取订阅权限、服务端下发消息。[1]

第二期需新增微信 `openid` 绑定、模板 ID 配置、用户订阅结果保存、发送结果回调、退订处理和频率限制。首期数据库中的 `notification_preferences` 可复用为用户意愿层，但不应假设其等同于微信授权结果。任何外部通知都不得包含完整账本、成本明细或敏感债务金额；通知内容应只说明事件和跳转入口。

## 7. 确认后实施顺序与验收标准

建议分两次交付。第一步只做站内消息与运营后台，第二步在微信小程序资质、模板和 OpenID 能力明确后再做订阅消息。这样既能尽快让后台“推送信息给客户”，又不会因平台授权或账本同步边界而产生不可靠的提醒。

| 阶段 | 开发内容 | 验收标准 |
|---|---|---|
| V1-A | 消息表、管理员权限、后台草稿/预览/发布/撤回、收件人快照 | 普通用户无法创建或读取他人消息；管理员可预览并发布给指定范围。 |
| V1-B | 消息中心、未读角标、详情 Sheet、已读/全部已读、顶部提示条 | 刷新后阅读状态仍正确；普通公告不在启动时弹窗；动作跳转正确。 |
| V1-C | 高风险确认弹窗规范与消息偏好 | 覆盖账本等操作有明确取消与动作名；关闭普通资讯不影响账本操作。 |
| V2 | 微信订阅消息、授权状态、模板发送和回调 | 仅向已明确授权且模板匹配的用户发送；拒绝/退订后不再发送。 |

建议开发时补充以下测试：管理员越权拦截、目标收件人快照不漂移、消息过期不展示、同一重要公告只自动提示一次、已读/关闭语义正确、操作跳转白名单校验、窄屏 390×844 下弹层不遮挡底部导航与主按钮。

## 8. 需要你确认的产品决策

请确认是否按以下 **推荐方案 V1** 开发：

1. 先做**站内消息中心和管理员后台**，不把普通公告做成启动弹窗；重要公告使用一次性顶部提示条，账本覆盖等风险操作才使用确认弹窗。
2. 后台首期支持“指定商户”与“全部已登录商户”两种范围；发送内容仅允许固定字段和应用内白名单跳转。
3. “经营建议”继续由用户本机账本实时计算，不在后台对本地数据进行推送判断；微信订阅消息列为第二期独立接入。

确认后，我会先实现 V1-A 与 V1-B，并提供管理员发送、商户收件、未读角标、弹窗节流及权限测试。

## References

[1]: https://developers.weixin.qq.com/miniprogram/dev/framework/open-ability/subscribe-message.html "微信小程序订阅消息（用户通过弹窗订阅）开发指南"
[2]: https://developers.weixin.qq.com/miniprogram/dev/framework/open-ability/subscribe-message-overview.html "微信小程序订阅消息概览"
[3]: https://m3.material.io/components/dialogs "Material Design 3 — Dialogs"
[4]: https://developer.apple.com/design/human-interface-guidelines/alerts "Apple Human Interface Guidelines — Alerts"
[5]: https://help.shopify.com/en/manual/fulfillment/setup/notifications/staff-notifications "Shopify Help — Setting up staff notifications"
