# “我的”模块完善方案：账号、行业与数据安全

**状态：待确认，不改动功能。**  
**目标：** 将当前“本机账本设置页”升级为小商户可理解、可恢复、可按行业调整的“经营与账户中心”。设计原则是：**先保护账，再连接账号；先切换模板，再改变输入；不自动合并会计数据。**

## 1. 现状与设计结论

当前“我的”页已能显示店铺名、行业模板、成本项目、隐形成本与资金成本，但本质仍是本机 `localStorage` 账本：用户没有可见的登录状态，数据管理入口没有实际动作；行业切换会更新默认分类与隐形成本分类，同时保留已有商品、材料、流水和销售。当前项目存在登录 URL 辅助函数，但实际服务端仅提供静态页面，没有可用的认证回调、会话查询或云端账本接口。

> 因此，**不能只在页面上新增“登录/退出”按钮**。如果没有账号归属、备份状态、冲突策略与退出后本机数据保护，用户会误以为账本已经同步，反而形成数据风险。

主流商户系统通常将“注册/登录”作为跨设备、员工/门店协同和数据同步的前提；行业方案则用于启用适合业务的商品属性、成本、价格与报表能力，而不是重置历史业务。秦丝强调跨手机、平板、电脑使用及多门店协同；网上管家婆将行业选择和采购/售价/费用分摊等业务能力分层；有赞将账户、店铺模板和多店数据管理分开处理。[1] [2] [3]

| 设计判断 | “算得清”的处理方式 | 原因 |
|---|---|---|
| 账号的第一价值 | **备份与跨设备恢复**，而不是强制登录 | 小商家可先低门槛记账，登录后获得数据保护 |
| 行业模板的定位 | **新录入的默认语言、分类和预设** | 不应改变历史流水、销售快照或已录成本 |
| 登录后的同步策略 | **先手动备份/恢复，后自动同步** | 会计数据自动合并可能重复流水、破坏成本快照 |
| 退出策略 | **只退出账号，不删除本机账本** | 退出不等于清空数据；清除必须单独危险操作 |

## 2. 推荐的信息架构

“我的”页不再平铺所有设置，而是以“账户与数据”为顶部主卡，将高频经营信息与低频安全设置分开。默认只显示三组，避免再次形成设置噪音。

| 区块 | 未登录状态 | 已登录状态 | 主操作 |
|---|---|---|---|
| **账户与数据** | `本机账本`、`尚未备份` | 头像/昵称、`已登录`、最近备份时间 | `登录并备份` / `立即备份` |
| **经营资料** | 店铺名称、当前行业 | 店铺名称、当前行业、账本归属 | `编辑店铺` / `切换行业` |
| **核算设置** | 成本项目、隐形成本、资金成本 | 同左 | 进入对应设置页 |
| **数据与安全** | 导出、导入恢复、清除本机数据 | 导出、恢复云端备份、退出登录 | 高风险操作二次确认 |

### 2.1 顶部账户卡

未登录时展示 **“本机账本”**、账本记录数和简短事实说明：`登录后可备份并在新设备恢复；当前账本不会自动上传。` 主按钮为 **“登录并备份”**，次按钮为 **“继续本机使用”**。这比“立即登录”更直接说明用户能获得什么。

已登录时展示账户昵称、最近成功备份时间和同步状态。第一期状态只使用三种：`尚未备份`、`已备份`、`备份失败`。避免在尚未实现实时同步时展示“已同步”这类会误导用户的词。

### 2.2 行业设置卡

行业入口显示为 `餐饮饮品 · 配方与损耗` 这类“行业 + 当前适配能力”的摘要，而非只显示行业名称。点击后进入四行业选择弹层；每个选项只写一行核心能力：

| 行业 | 新录入默认分类 | 商品成本表达 | 快速成本两项 |
|---|---|---|---|
| 餐饮饮品 | 食材、包装、平台、人工 | 配方 | 食材成本 + 包装/人工 |
| 社区零售 | 货品、物流、促销、房租 | 进货明细 | 进货价 + 配送/让利 |
| 商贸摆摊 | 进货、摊位、交通、尾货 | 货品成本 | 拿货价 + 交通/尾货 |
| 手作生产 | 材料、包材、工时、工具 | 制作成本 | 材料成本 + 人工/包材 |

切换前必须展示一张简短确认卡：

> **将改变：** 后续新增流水的默认分类、商品成本名称、快速成本预设、模板默认隐形成本分类。  
> **不会改变：** 已有商品、材料、流水、销售、成本版本、销售成本快照和自定义分类。

## 3. 行业模板的安全响应规则

当前行业切换已经保留商品、材料、流水和销售，这是正确的基础。下一版应把“模板设置”和“用户覆写”显式区分，避免模板切换悄悄覆盖用户已配置的核算口径。

| 对象 | 切换行业后的规则 | 是否保留历史 |
|---|---|---|
| 默认成本分类 | 替换为新行业的默认可选项；自定义分类追加保留 | 是 |
| 历史流水分类 | 保持原文字与金额 | 是 |
| 商品、材料、BOM、成本版本 | 不修改；原商品继续按原版本可追溯 | 是 |
| 新商品成本文案/快速预设 | 使用新行业的名称、单位和两项成本 | 不适用 |
| 隐形成本默认分类 | 仅在“跟随模板”模式下更新 | 是 |
| 用户手工选择的隐形成本分类 | 标记为“自定义”，切换行业不覆盖 | 是 |
| 资金成本口径 | 始终只保留“利息和融资费”规则 | 是 |

为实现上述规则，建议将 `hiddenCostCategory` 扩展为 `template` 或 `custom` 来源。当前切换行业会直接替换该分类；改造后，只有来源为 `template` 才随模板更新。商品已有的 `costCategory`、材料、BOM 与销售成本快照则保持不变。

## 4. 登录、备份、恢复与退出

### 4.1 账号实现边界

推荐直接接入项目已经具备的 **Manus OAuth 账号能力**，不另造手机号/密码体系。第一期只做“一个账号对应一个主账本”，不引入员工、角色、多店或账套切换，避免把商户账本复杂化。

云端保存的最小结构应是：`userId`、`ledgerJson`、`schemaVersion`、`industry`、`revision`、`updatedAt` 与 `backedUpAt`。完整账本 JSON 可以保留现有销售成本快照、BOM 版本和历史字段；`schemaVersion` 用于未来数据迁移；`revision` 用于发现新旧设备同时修改的冲突。

### 4.2 首次登录流程

首次登录成功后，不应立即覆盖数据，而应进入“账本归属确认”：

1. 若云端无账本，显示本机账本摘要，用户点击 **“备份此设备账本”** 后创建首个云端版本。
2. 若云端已有账本而本机为空，用户点击 **“恢复云端账本”**。
3. 若两端都有账本，显示更新时间、流水数、商品数和销售数，用户必须选择 **“保留本机并新建云端版本”** 或 **“使用云端覆盖本机”**；第一期不做自动合并。

这种显式选择比直接“同步”更适合账务场景，因为两份流水无法安全地仅按时间戳合并。

### 4.3 退出与清除

退出登录只清除会话，**不删除当前设备账本**。退出确认文案固定为：`退出后，本机账本仍保留；其他人打开本设备仍可查看。` 如果用户需要彻底移交设备，另设危险操作 **“清除本机账本”**，要求输入店铺名确认，并提示先导出或确认云端备份成功。

## 5. 分阶段开发建议

| 阶段 | 范围 | 交付结果 | 不纳入范围 |
|---|---|---|---|
| **A. 账户与安全底座** | 激活 OAuth、账户卡、登录/退出、账本数据库表、手动备份 | 可登录、可退出、可看备份状态 | 自动同步、多门店、员工权限 |
| **B. 账本恢复保护** | 导出 JSON、导入校验、云端恢复、冲突选择 | 换设备或误操作后可恢复 | 两端自动合并 |
| **C. 行业安全切换** | 切换前影响说明、模板/自定义来源标记、行业摘要 | 切换行业不覆盖用户成本口径 | 自动重分类历史流水 |
| **D. 体验增强** | 店铺资料编辑、最近备份、网络失败重试 | “我的”模块完整可用 | 员工、连锁、多账本 |
| **E. 后续扩展** | 多账本、员工权限、跨设备冲突中心 | 适用于成长型商户 | 本轮不开始 |

## 6. 建议的验收标准

账号登录成功后，用户能看到明确身份和“尚未备份/已备份”状态；退出后会话消失但本机账本仍在。首次同步遇到两份账本时，系统不会自动覆盖任一账本。行业切换能立刻改变新录入的默认分类、成本名称和快速成本预设，但不改变历史流水、销售、成本版本或用户自定义分类。

所有高风险操作均须可测试：登录失败、网络失败、重复点击备份、云端与本机冲突、退出后重新进入、清除本机数据取消/确认、模板与自定义隐形成本分类切换。账务回归必须继续验证销售成本快照、跨月汇总、材料价格变动和行业切换后的新增流水。

## 7. 待确认的开发范围

推荐先实施 **A + B + C 的最小安全版**：账号登录/退出、手动备份与恢复、本机/云端冲突确认、行业切换保护。这样能先解决“数据安全吗、登录后会发生什么、行业切换会不会破坏账”的核心疑问。

请确认以下两点后再开始开发：

1. 是否同意第一期采用 **Manus OAuth 登录**，而不新增手机号/验证码登录？
2. 是否同意第一期采用 **手动备份/恢复**，不做自动跨设备实时同步和自动合并？

## References

[1]: https://www.qinsilk.com/invoiceIntroduction/mobile.html "秦丝进销存：移动端、跨设备与多门店能力说明"
[2]: https://www.wsgjp.com.cn/Products/Jxc.aspx "网上管家婆进销存：行业解决方案、售价与费用分摊能力说明"
[3]: https://www.youzan.com/solutions/weixinxiaodian "有赞微信小店：账户、店铺模板与多店数据管理说明"
