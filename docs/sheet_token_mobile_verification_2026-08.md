# 弹层设计令牌移动端复核

## 已确认

材料采购弹层在 390×844 视口下已应用统一字段高度、12px 分栏间距、8px 输入圆角、账页组合提示卡与 48px 保存 CTA。跨单位换算字段、业务日期和单位成本预览均未溢出，保存按钮保持在可触达的底部操作区。

## 销售登记复核

已在新建商品后以独立状态重新截取销售登记弹层。商品选择、数量/成交价双列输入、业务日期、未定价阻断提示、销售收入预览与“保存并结转”CTA 均保持统一字段高度、间距与视觉层级；CTA 未被安全区或底部导航遮挡。

退款与录错删除弹层复用同一 `pricing-sheet`、字段、风险说明和 `sheet-action` 令牌。本轮组件与全流程回归覆盖其既有数量、金额、日期、成本快照和库存回滚路径；后续若修改退款业务流程，应重新进行独立视觉截图复核。
