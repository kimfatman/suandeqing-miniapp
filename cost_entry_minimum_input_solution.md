# 商品成本极简录入方案

**状态：仅分析与方案设计，不改动现有功能。**  
**设计约束：预设能预设的一切；在“快速录入”中，用户最多填写两个金额项目；不以猜测或演示金额污染正式账本。**

## 一、结论

当前“新增项目”功能并未从代码中删除，但它在实际界面中处于**低可发现性路径**：用户先在“商品”页进入“编辑进货明细/编辑配方”，再滚动到编辑弹层下部，把默认的“已有材料”切换成“自定义成本项目”，按钮才会变为“新增自定义项目”。新增后还需填写名称、单位、单价、数量四个字段。对小商家而言，这会被理解为“没有新增入口”或“录入太麻烦”。

本方案建议将商品成本重构为两个层级：默认使用 **“30 秒快速录入”**，只填一个必填金额和一个可选金额；需要按材料、损耗、出成量精细核算的用户，再主动展开 **“进阶明细”**。这样保留已有 BOM、成本版本和销售快照能力，同时把首次核算的操作压缩为最低必要量。

> **预设的是成本名称、归类、单位和计算规则，不是金额。** 正式账本中的金额必须由用户输入或由已有真实采购/流水带入，不能用行业示例数值自动填充。

## 二、现状诊断

| 观察项 | 当前状态 | 对用户的影响 | 判断 |
|---|---|---|---|
| 商品页入口 | 商品卡片只有“编辑进货明细/编辑配方”等行业化编辑按钮 | “编辑”不等于“新增”；空成本商品虽会显示“补齐”，已核算商品的新增意图被弱化 | 应改为明确的“录入/补充成本”主入口 |
| 新增项目入口 | 在成本编辑弹层底部，默认选中“已有材料” | 用户要先滚动、再理解下拉选项、再切换类型 | 技术上存在，体验上近似不可见 |
| 自定义项目字段 | 名称、单位、单价、数量共四项 | 用户需要决定名称与单位，且要理解数量×单价关系 | 对“只想知道这件赚不赚钱”的商户过重 |
| 行业模板 | 已有餐饮、零售、摆摊、手作四套成本分类和行业文案 | 预设能力已具备，但尚未转化为低输入的成本卡片 | 可以直接复用 |
| 核算基础 | 已有 BOM 版本、成本快照、材料改价与销售结转保护 | 不能为简化体验而绕过或重写历史成本 | 必须保留为底层账务边界 |

## 三、推荐信息架构：一主一辅

### 1. 默认模式：30 秒快速录入

用户只看到两张成本卡，且**最多填写两个金额**。不让用户填写项目名称、单位或数量；系统按当前行业预设这些信息。

| 成本卡 | 是否必填 | 用户操作 | 系统预设 | 核算归属 |
|---|---:|---|---|---|
| **主成本** | 是 | 输入“每件/每份的原料或进货成本”一个金额；也可一键采用已录入材料的真实单价建议 | 行业化名称、单位“件/份”、主成本分类 | 直接成本 |
| **附加成本** | 否 | 输入“每件/每份额外成本”一个金额；不填即按 0 处理 | 行业化标签、单位“件/份”、对应计算方式 | 直接成本或经营分摊 |

快速模式的保存门槛只有一条：**主成本必须为大于 0 的真实金额**。附加成本是可选的，默认不计入，不把“未填”伪装成“系统估算”。

### 2. 进阶模式：完整明细

在快速模式底部保留低强调入口：`已有多种材料、损耗或批量制作？展开进阶明细`。进入后，继续使用现有材料 BOM、自定义项目、损耗率、出成量、历史版本恢复等能力。它服务需要准确拆料的商户，但不应阻塞第一次建立商品成本。

现有详细 BOM 不应被自动转换或覆盖；有历史成本版本的商品默认仍以“编辑进阶明细”打开，除非用户主动切换回快速模式并明确确认将产生新的成本版本。

## 四、四类行业的预设映射

| 行业 | 主成本卡（必填） | 附加成本卡（可选） | 由系统/账本自动处理，不放进商品首屏 |
|---|---|---|---|
| 餐饮饮品 | `食材成本 / 份` | 默认 `包装费 / 份`；可切换为 `单件人工` | 房租水电、平台抽佣、店主工时等期间成本，按既有分摊规则进入经营/完整成本 |
| 社区零售 | `进货价 / 件` | `单件配送费 / 件` | 平台费率应来自定价设置；促销、货架与租金来自流水或期间分摊 |
| 商贸摆摊 | `拿货价 / 件` | `单件摊位/交通分摊` | 月度摊位费、交通和尾货折价应由账本流水归集，避免每个商品重复手填 |
| 手作生产 | `材料成本 / 件` | `直接人工 / 件`；可切换为 `包材费` | 工具折旧、试做报废等进入期间成本或进阶明细 |

附加成本卡只允许在行业预设标签中切换，不允许现场新建名称。这样既保证最多两个金额输入，也避免“平台服务费”“配送费”“人工”等同类项目被用户重复命名、重复计算。

## 五、建议的页面流程

| 阶段 | 页面表达 | 用户输入 | 系统动作 |
|---|---|---|---|
| 商品详情 | 将当前“编辑进货明细”替换为更直白的主按钮：`录入成本`、`补充成本`或`更新成本` | 无 | 根据是否已有成本决定按钮文案；旁边显示“约 30 秒” |
| 快速成本页 | 标题：`先填两项，先算清一件赚不赚` | 主成本金额；可选附加成本金额 | 自动带入行业名称、成本归类与单位；实时预览直接成本 |
| 真实材料建议 | 主成本卡下方显示“最近采购：鲜牛奶 ¥x/毫升”或“最近进货：商品 A ¥x/件” | 点击“采用”即可，无需手抄 | 只在有真实材料/采购记录时显示；点击才采用，绝不自动写入金额 |
| 保存结果 | `已生成本次成本版本` | 无 | 生成当前商品的新成本版本；历史销售继续使用原快照 |
| 进阶路径 | 弱化链接：`展开材料、损耗与批量明细` | 按需录入 | 打开现有 BOM 编辑能力 |

## 六、会计口径与数据模型边界

快速模式不能新建“随意命名的成本项目”。名称与单位由行业模板决定；需要自定义类别的用户仍应在“我的 → 成本项目”中统一管理，以避免每个商品重复建立口径不同的“杂费”“平台费”“运费”。

实现时可将快速输入转换为现有可追溯结构：主成本写入一个系统标记的成本项；附加成本依据类型进入包装、直接人工或系统标记的单件附加成本；再调用当前成本版本快照逻辑。该映射必须保证同一笔附加成本只进入一个位置，避免同时写入 BOM 与包装/人工字段而重复计算。

固定成本、房租、设备折旧、月度配送费、借款利息和平台费率不应要求逐个商品填写。它们要么来自已有流水并按期间分摊，要么来自统一的经营/定价设置。这样商品页只解决“每卖一件必然发生的成本”，经营页解决“本月整体发生的成本”。

## 七、分阶段实施建议

| 优先级 | 改动 | 目的 | 验收标准 |
|---|---|---|---|
| P0 | 恢复显性入口：商品详情主按钮统一为“录入/补充成本”，弹层顶部增加“快速录入” | 解决“入口没有了”的感知问题 | 从商品页到可填写成本不超过 1 次点击；不需要滚动寻找“新增项目” |
| P0 | 增加一主一辅快速模式 | 把首轮录入压缩到最多两个金额 | 快速模式无成本名称、单位、数量文本输入；主成本必填、附加成本可空 |
| P0 | 保留“进阶明细”并保护历史版本 | 不牺牲准确核算与历史可追溯 | 材料改价、保存新成本、历史销售、恢复版本的现有测试继续通过 |
| P1 | 接入真实采购/材料的“采用最近单价”建议 | 减少重复抄数 | 仅在真实材料存在时显示；必须由用户点击采用；不覆盖已手填金额 |
| P1 | 将附加成本标签做成行业预设切换 | 覆盖包材、人工、配送等常见场景 | 单个商品快速模式仍最多两个金额输入，且标签切换不产生重复项 |
| P2 | 为用户提供“从相似商品复制成本” | 降低多商品店铺的重复工作 | 复制后创建新的成本版本，原商品与历史销售不变 |

## 八、明确不建议做的事情

不建议重新在商品成本页开放自由的“新增项目名称 + 单位 + 单价 + 数量”作为默认路径；这正是当前高负担的来源。也不建议按照行业自动填写具体成本金额，或把演示材料价格带入正式账本；两者都会伤害核算准确性。

不建议把房租、月度人工、设备折旧、利息等所有成本塞入每个商品的直接成本编辑器。它们应当从经营账本归集和分摊，否则用户会重复填报、重复计入，违背当前系统已建立的成本去重和销售快照边界。

## 九、建议采用的决策

建议下一版采用：**默认“快速录入”，一个必填主成本金额 + 一个可选行业附加成本金额；完整材料明细仅在用户主动展开时显示。** 这套方案能直接消除入口不明显、四字段自定义行过重、成本项目命名混乱三类问题，同时保留现有真实材料、BOM、损耗、成本版本、销售结转与历史快照能力。
