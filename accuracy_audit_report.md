# “算得清”核算准确性全面审计报告

**审计目标：** 以核算准确性、口径可解释性和结果可追溯性为第一优先级，检查当前微信小程序MVP的成本、经营、债务和定价能力。

**审计范围：** `client/src/lib/costEngine.ts`、`client/src/lib/ledgerStore.ts`、`QuickRecordSheet.tsx`、`BomEditorSheet.tsx`、`PricingPanel.tsx`、`Home.tsx` 中的材料采购、隐形成本、资金成本、账本聚合和定价入口，以及当前自动化测试。

## 一、审计结论

当前版本已经具备成本分层、BOM重算、快速记账、债务分类和定价公式的产品骨架，但还不能把首页的“经营利润估算”视为严格意义上的利润。最大的原因是：**收入和支出流水采用现金收付口径，而商品成本采用单品成本口径，两者尚未通过销售数量和销售商品建立会计上的对应关系。**因此当前首页的核心结果更准确的名称应是“账本结余”或“经营现金结余”，不应直接称为“经营利润”。

此外，材料单位、损耗、成本分摊、历史快照和资金成本归集尚未形成完整模型。尤其是**手工录入的每份资金成本可能与账本中已录入的借款利息重复计入**，这是当前最需要优先修复的准确性风险。

> **总体评级：需要进行P0准确性修复后，才适合用于真实经营决策。** 当前版本适合作为交互原型和账本录入演示，不适合作为正式财务核算或对外报表依据。

## 二、关键风险总表

| 编号 | 风险 | 严重度 | 当前影响 | 建议优先级 |
|---|---|---:|---|---:|
| A1 | 首页把现金收支差额称为经营利润 | P0 | 用户可能误把现金结余当作商品利润 | 立即修复 |
| A2 | BOM单位体系不完整，采购单位固定为“盒” | P0 | 克、千克、毫升、升、箱、个无法可靠换算 | 立即修复 |
| A3 | 采购数量为空或为0时静默按1计算 | P0 | 单位成本可能被放大或被错误接受 | 立即修复 |
| A4 | 种子商品成本与当前BOM不一致 | P0 | 首次打开与保存配方前后结果不同 | 立即修复 |
| A5 | 隐形成本和资金成本是全局单值，没有期间和分摊依据 | P0 | 所有商品被使用同一个每份金额，结果缺乏可追溯性 | 立即修复 |
| A6 | 资金成本可能与流水中的利息、融资费重复计算 | P0 | 完整成本和定价建议被高估 | 立即修复 |
| A7 | BOM没有损耗、出成率、重复材料和版本快照 | P0 | 配方成本无法覆盖餐饮和手作的真实损耗 | 立即修复 |
| A8 | 定价输入缺少目标范围、费率校验和当前售价对比 | P1 | 可能生成不合理建议价，用户不易发现原因 | 高优先级 |
| A9 | 账本数据只在localStorage，且加载没有结构校验 | P1 | 换设备、清缓存、旧版本字段变化会导致数据丢失或异常 | 高优先级 |
| A10 | 只有一条聚合测试，缺少成本与定价测试 | P1 | 公式改动容易回归 | 高优先级 |
| A11 | 快速记账没有日期、凭证、编辑、删除和重复保护 | P1 | 月度核算和审计追溯能力不足 | 高优先级 |
| A12 | 没有销售商品数量，单品成本无法汇总到真实利润 | P1 | 商品毛利与首页经营结果断裂 | 高优先级 |

## 三、公式核验结果

### 1. 单品成本公式

当前成本引擎的结构为：

```text
直接成本 = 材料BOM成本 + 包装 + 直接人工
经营成本 = 直接成本 + 固定费用 + 隐形成本
完整成本 = 经营成本 + 资金成本
```

这个分层结构本身清晰，但实现中的“固定费用、隐形成本、资金成本”都是单个全局数值，未记录期间、商品、销量、收入或分摊基数。因此公式形式正确，数据来源不足，结果不能被视为准确的实际成本。

### 2. 默认商品的可复核偏差

根据当前种子材料和配方：

```text
材料成本 = 18 × 0.036 + 280 × 0.009 + 25 × 0.012 = 3.468 元
直接成本 = 3.468 + 0.48 + 0.60 = 4.548 元
```

但当前种子商品把 `direct` 设为 **5.60元**。如果按代码中的当前BOM重新计算，直接成本应为4.548元（金额展示为4.55元），而不是5.60元；如果此前材料数量或单位定义另有隐含成本，则代码没有保存该依据。当前商品的`operating`初始值为7.82元，按现有固定费用0.92元和隐形成本1.30元重新计算应为6.768元（金额展示为6.77元）。由此可见，**默认展示值与BOM公式存在不一致**，会造成用户第一次看到的成本与保存配方后的成本跳变。

### 3. 定价公式

当前目标利润率模式的公式为：

```text
建议售价 =（成本 + 每单固定费用）÷（1 − 平台/支付费率 − 目标利润率）
```

目标单份利润模式为：

```text
建议售价 =（成本 + 每单固定费用 + 目标单份利润）÷（1 − 平台/支付费率）
```

两条公式在“费率以售价百分比收取、成本已完整且不重复、目标利润率定义为售价占比”的前提下成立。当前实现的问题在于：`target`、`feeRate`和`fixedFee`缺少严格边界校验；费率或目标利润率接近100%时只返回无效，没有说明缺少多少安全空间；取整步长固定为0.5元，且结果没有展示取整前后的利润变化。

当前定价面板也没有读取选中商品名称，标题固定为“招牌奶茶”；没有展示当前售价与建议售价差额；没有让用户输入市场参考价；无法判断当前售价是否已经覆盖完整成本。

### 4. 债务成本口径

当前流水聚合正确地把“本金还款”排除在经营成本外，同时保留在现金流流出中；借款利息和融资服务费计入经营支出及资金成本。这一口径方向正确。

但商品完整成本又额外读取全局`fundingCost`，而`QuickRecordSheet`允许用户再次录入借款利息和融资服务费。两者之间没有借款账户、期间或归集关系，因此同一笔利息可能既被录入流水，又被手工填入每份资金成本，形成重复计费。必须在模型中明确：**资金成本只能来自一种来源，或者显示已归集金额并禁止重复添加。**

## 四、数据模型和业务流程风险

### 1. 单位和采购成本

材料只有名称、单位和单位成本，没有采购单位、库存单位、换算系数、采购批次和供应商价格历史。材料录入页把单位硬编码为“盒”，并使用`Math.max(quantity, 1)`处理数量为空或为0的情况。这不是容错，而是把用户的错误输入静默转换成合法结果，应该改为阻止保存并要求重新输入。

建议至少建立以下字段：采购数量、采购单位、使用单位、换算系数、含税/不含税标记、运费、采购批次、有效日期和供应商。系统还应明确采用移动加权平均、先进先出或指定批次成本，不能让用户误以为一个固定单位成本代表全部库存。

### 2. BOM、损耗和出成率

当前BOM只保存材料ID和数量，允许同一材料重复加入，数量可以为0，材料删除后只显示“已停用材料”，仍然以0成本参与计算。更重要的是没有损耗率、出成率、试做报废、赠品和批次产量。例如一锅原料做出10杯，成本应按实际出成量分摊，而不是简单按配方数量相加。

建议将BOM升级为“生产批次配方”：投入数量、损耗率、预计出成数量、单位、有效版本、生效日期和停用日期都必须保存。保存新配方时生成新版本，不覆盖旧版本。

### 3. 隐形成本和分摊

当前隐形成本是一个“每份分摊金额”，资金成本也是一个“每份资金成本”。这会让所有商品无差别增加相同金额，无法解释房租、店主人工、配送、平台服务费、设备折旧和利息分别如何分配。

建议每条间接成本记录包含金额、期间、成本类型、分摊方式和分摊基数。可选分摊方式包括按销量、销售额、工时、占用面积、订单数或手工指定。系统需要同时展示“原始金额”和“分摊到当前商品的金额”，让用户能追溯结果来源。

### 4. 收入、销售和利润

当前流水只有收入/支出、金额、分类、备注和日期，没有商品ID、销量、售价、退款、折扣、平台订单费或销售渠道。因此首页的`收入 - 支出`只能叫现金结余，不能叫商品经营利润；它没有把销售商品对应的成本结转出来，也没有处理库存变化。

建议增加销售记录：商品、数量、成交单价、折扣、退款、渠道费、支付费和销售日期。商品毛利应按销售数量乘以当时生效的单位成本计算，经营利润再扣除期间间接成本，现金流则另行按实际收付款计算。

## 五、输入、持久化和追溯性审计

| 检查项 | 当前状态 | 风险 |
|---|---|---|
| 金额为负数 | 快速记账拦截小于等于0；其他编辑入口不完整 | 不同入口可能产生不同结果 |
| 空数量/零数量 | 材料录入静默按1 | 直接导致单位成本错误 |
| 小数精度 | 最后统一四舍五入，但中间计算未统一金额策略 | 多次累加可能出现分币差异 |
| 费率边界 | 定价公式只检查分母，未限制0–100% | 用户可输入不合理费率 |
| 目标利润率 | UI没有最大值约束 | 可输入超过100%或极端小数 |
| 日期 | 快速记账固定为当天 | 无法补录历史业务，月报不准确 |
| 编辑/删除 | 主要是新增 | 错录后只能继续增加反向记录，追溯不完整 |
| 数据校验 | localStorage JSON直接浅合并 | 旧版本或损坏数据可能进入计算层 |
| 历史快照 | BOM说明声称会保存版本，但数据模型没有版本实体 | 修改配方会改变当前商品，历史结果无法复原 |
| 多设备同步 | 仅本地浏览器存储 | 换设备、清缓存或多端使用会丢数据 |

## 六、自动化测试审计

当前自动化测试只有一条` summarizeLedger` happy path，覆盖收入、支出、利息、本金还款和日期聚合。尚未覆盖以下核心准确性场景：BOM重算、材料单位成本、损耗率、定价两种模式、目标利润率边界、费率边界、取整、负数/空值、隐形成本修改、资金成本重复计费、localStorage恢复、历史快照、跨天趋势和销售数量。

建议把测试分成四层：纯公式单元测试、账本聚合测试、持久化兼容测试和端到端用户流程测试。每次修复成本口径都必须同时验证利润率、建议售价、经营利润和现金流四个结果，避免只修复一个数字而改变另一个口径。

## 七、P0/P1/P2修复路线

| 优先级 | 必须完成的能力 | 验收标准 |
|---|---|---|
| **P0** | 把首页“经营利润估算”改为“经营现金结余”，或先实现销售记录与商品成本结转 | 现金结余、商品毛利和经营利润三者分别显示，口径不混用 |
| **P0** | 建立单位换算和采购数量校验 | 1箱、12盒、500克等输入可转换到使用单位；0和空数量不可保存 |
| **P0** | BOM支持损耗、出成量和版本快照 | 修改配方后旧版本可复原，历史销售不被新成本覆盖 |
| **P0** | 取消资金成本重复计费 | 录入利息后，完整成本只归集一次，并能显示来源 |
| **P0** | 建立间接成本分摊模型 | 每项房租、人工、配送和平台费能显示期间、分摊基数与商品归属 |
| **P1** | 完善定价边界与解释 | 目标利润率、费率和固定费都有范围校验；展示取整前后差异与当前售价对比 |
| **P1** | 加入销售、退款、折扣和渠道费 | 商品毛利能按销售数量和成本版本计算 |
| **P1** | 增加编辑、删除、撤销和日期补录 | 错账可修正且有操作记录，不需要用反向流水掩盖错误 |
| **P1** | 扩充自动化测试 | 覆盖上述P0公式和所有边界输入 |
| **P2** | 云端同步、供应商批次、库存和多店权限 | 多设备数据一致，采购批次和库存成本可追溯 |
| **P2** | 经营异常诊断 | 能解释成本上涨来自材料、损耗、平台费还是资金成本 |

## 八、建议的核算对象分层

在下一版数据模型中，建议明确区分四个对象：**流水**记录真实收付款；**销售**记录商品和数量；**成本版本**记录某个期间的BOM和分摊结果；**现金流事件**记录本金、利息和实际支付。首页只做聚合展示，不直接把这些对象混成一个`expenses`数字。

推荐的核心链路是：

```text
采购批次 → 单位换算 → BOM版本 → 商品单位成本
销售记录 → 销量 × 生效成本版本 → 商品毛利
期间间接成本 → 分摊规则 → 经营利润
借款事件 → 利息/费用/本金 → 资金成本与现金流
```

## 九、最终判断

当前产品最重要的工作不是继续增加更多图表，而是先解决**现金结余、商品毛利、经营利润和资金现金流四个口径分离**的问题。完成P0后，用户看到的每个数字都应能回答三个问题：它来自哪笔业务？使用了哪个成本版本？是否已经包含或排除了资金成本与本金？

在P0修复完成前，建议在界面显著位置标注“经营分析估算，不替代正式会计和税务处理”，并把所有行业默认值标识为“估算”。

## 十、审计验证结果

本轮新增了成本引擎测试，覆盖目标利润率反推、目标单份利润反推、平台费率计算、价格向上取整、负固定费、负目标值和利润率加费率达到100%的无效边界；账本聚合测试覆盖收入、支出、利息、本金还款、现金结余与经营结果分离、单位换算和种子BOM重算；组件交互测试覆盖材料与BOM表单在0数量时显示错误且不调用保存回调。当前共23个测试用例全部通过，TypeScript检查和生产构建也已通过。

本轮已完成的修复包括：原材料采购金额/数量/换算系数严格正数校验；BOM用量和材料引用严格校验；采购单位到使用单位的基础换算；带BOM商品加载时按当前配方重算，避免种子成本跳变；首页明确区分现金结余与未按销量结转的经营结果估算；定价输入增加负数与费率边界校验；新增销售记录并绑定单位直接成本、间接成本、资金成本和成本期间快照；按期间筛选销售与融资费用，并支持隐形成本手工/流水来源去重；BOM增加损耗率、批次出成量和完整成本版本快照，历史恢复后材料改价仍保持快照成本。需要强调的是，测试通过只说明当前实现与当前公式一致；localStorage兼容、多设备同步和正式会计/税务口径仍需在后续云端化阶段继续验证。
